import React from 'react';

export default (props) => (
  <div className="card" style={{ margin: '0 auto', width: 600 }}>
    <div className="card-content">
      <p className="title">The page that you were looking for isn't there.</p>
    </div>
  </div>
);
